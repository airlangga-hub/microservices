meta {
  name: Become Seller
  type: http
  seq: 1
}

patch {
  url: http://localhost:8080/api/become-seller
  body: none
  auth: inherit
}

script:post-response {
  bru.setVar("user_token", res.body.token);
}

settings {
  encodeUrl: true
  timeout: 0
}
